# Install in a dedicated virtual environment; these are not Node.js backend dependencies.
# ragas 0.4.3 imports a VertexAI module removed by modern LangChain packages.
# Keep this complete LangChain/Ragas set aligned until that upstream issue is fixed.
ragas==0.3.9
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-openai>=0.3,<0.4
python-dotenv>=1.0,<2.0
